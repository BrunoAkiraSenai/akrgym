---
date: 2026-09-09
type: development-log
tags: [akrgym, development, audit]
ai-first: true
---

# AkrGym — checkpoint de correções

## Escopo

Checkpoint da continuação da auditoria [[01 - Projetos/AkrGym/AkrGym — Auditoria 2026-09-08]]. O trabalho foi consolidado na branch `dev`, commitado e publicado no PRD. Nenhuma credencial foi registrada neste cofre.

## Verificado

- O diário alimentar foi exercitado no harness local em tela de 390px: duas confirmações consecutivas resultaram em `2 / 4`; calorias, macros e fibras foram atualizados.
- `npm run test:unit`: 39 testes aprovados.
- `npm run lint`: aprovado após correção sintática do checkpoint.
- `npm run build`: aprovado nesta sessão.
- `npm run test:rules`: 28 testes aprovados no emulador Firestore (inclui rejeição de mês/dia impossíveis no diário).
- `npm audit --omit=dev` continua com 5 vulnerabilidades transitivas (2 críticas, 2 altas, 1 moderada); não foi executado `audit fix --force`. A triagem permanece pendente.
- A lógica de ritmo foi ajustada para 14 pontos, cada um representando uma janela móvel de 7 dias; 4 dias ativos na janela equivalem a 100%. A tendência compara o ponto atual ao ponto de 7 dias antes.

## Alterações consolidadas e publicadas

- [`src/utils/dietDiary.js`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/utils/dietDiary.js): normalização finita de nutrientes, snapshots históricos e operações de diário transacionais.
- [`src/components/pages/Dieta.jsx`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/components/pages/Dieta.jsx): listener em tempo real, ações direcionadas por ID, proteção contra edição/IA obsoleta e contagem de fibras/extras.
- [`src/utils/fitness.js`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/utils/fitness.js) e [`src/components/pages/Home.jsx`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/components/pages/Home.jsx): ritmo real e nome da última rotina salvo no histórico.
- [`src/utils/workoutSession.js`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/utils/workoutSession.js) e [`src/components/pages/Execucao.jsx`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/components/pages/Execucao.jsx): validação de sessão, rascunho com fingerprint e gravação idempotente. Este bloco precisa de uma nova revisão de integração antes de ser considerado concluído.
- [`src/components/pages/Evolucao.jsx`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/components/pages/Evolucao.jsx): carregamento sem limite inicial e validação numérica/data; requer teste de UI posterior.
- [`src/components/Layout.jsx`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/components/Layout.jsx) e [`src/components/ConfirmModal.jsx`](<CAMINHO_LOCAL_DO_REPOSITORIO>/src/components/ConfirmModal.jsx): safe-area/nav acessível e foco/Escape do modal.

## Commit e deploy

- Commit `de3992e` (`Consolidar auditoria e endurecer fluxos do AkrGym`) enviado para `origin/dev`.
- Firebase Hosting publicado com sucesso em https://akrgym.web.app.
- Regras do Firestore (`firestore.rules`) compiladas e liberadas no projeto `akrgym`.
- Smoke HTTP pós-deploy: Home, `robots.txt` e `sitemap.xml` responderam `200`.
- Validações: 39 testes unitários, 28 testes de regras, lint, build e `check:production` aprovados.
- A autenticação interativa do Firebase CLI falhou no navegador; o deploy foi concluído usando a conta de serviço local já existente. O arquivo e seus valores não foram commitados nem copiados para o cofre.

## Pendências explícitas

- A auditoria completa ainda não terminou. O Worker de IA, regras de schema C06, App Check, timeout/rate limit distribuído, migração de convidado, CSP/SPA soft-404, CI/deploy automático via `main` e triagem de dependências continuam pendentes.
- O teste concorrente foi acrescentado ao arquivo de regras, mas a compatibilidade do helper de transação com o emulador precisa ser revalidada; a suíte passou sem falhas nesta execução.
- O harness local foi alterado para simular `onSnapshot`/`runTransaction`; ele é evidência de comportamento de interface, não prova de produção. No cenário lento, não houve gravação antes da configuração carregar; uma alteração durante IA tardia exibiu aviso e preservou os macros; no cenário normal, editar alimentos e clicar imediatamente em “Calcular IA” aplicou o resultado sem falso descarte; sem sessão, a tela Login foi exibida.
- O commit e o deploy descritos acima foram realizados; não houve alteração de credenciais.

## Próxima retomada

1. Reabrir este checkpoint e verificar `git status`/diff antes de editar.
2. Corrigir qualquer regressão de integração em `Execucao.jsx` e executar browser smoke test (dieta, treino, evolução, modal e viewport 320/390/1280).
3. Atualizar a nota de auditoria somente com evidência reproduzível; depois prosseguir para regras/Worker e CI.

Confiança: alta para as suítes executadas; média para integração visual ainda não coberta.

## Ajuste visual em QA (2026-09-09)

- A tela de Diário foi refinada na branch `QA`: nutrientes das refeições agora aparecem em uma faixa única e legível; refeições confirmadas exibem o retorno "Incluída no progresso alimentar".
- O bloco de alimento extra ganhou hierarquia, métricas alinhadas em cinco colunas e ações mais claras.
- O painel de IA foi unificado ao acento ciano/verde do produto, com indicação de "Resultado da IA" para revisão antes de adicionar.
- `npm run lint`, `npm run test:unit` (39) e `npm run build` passaram. O ajuste inicial foi posteriormente consolidado junto com a evolução visual da tela.

### Redesign da Dieta inspirado na referência (QA)

- O cabeçalho agora combina título/subtítulo, seletor de data acessível e atalhos de configuração sem remover o fluxo existente.
- A meta diária ganhou um card próprio; o resumo foi reorganizado em um único card com anel de calorias e quatro barras de macros (proteína, carboidratos, gorduras e fibras), sempre alimentadas pelos totais reais do diário.
- Cada refeição passou a ter ícone contextual por tipo, horário, status, faixa de nutrientes (incluindo fibras) e confirmação visual quando entra no progresso alimentar.
- O alimento extra e a análise por IA ganharam hierarquia própria, ações mais legíveis e layout responsivo que empilha título/ações em larguras pequenas.
- A arte contextual das refeições é discreta e construída com os ícones do design system, evitando imagens decorativas pesadas e respeitando as variáveis de tema existentes.
- A regra de cálculo não foi alterada: confirmação, alimento extra, edição, exclusão e fibras continuam passando pelas funções existentes do diário.
- Na revisão final dos cards de refeição, a faixa de nutrientes foi compactada em chips inline, o status pendente ganhou contraste consistente, a borda foi suavizada e uma seta lateral discreta foi adicionada apenas como linguagem visual. Nenhuma imagem de fundo foi introduzida.
- As abreviações visuais de nutrientes foram substituídas por quatro ícones semânticos: Beef para proteína, Wheat para carboidratos, Droplets para gorduras e Leaf para fibras. Os rótulos completos do resumo permanecem inalterados; somente os marcadores abreviados foram trocados.
- Os alimentos extras agora reaproveitam a composição das refeições fixas: ícone, nome, subtítulo de origem, status `Incluído`, chips de nutrientes com ícones e ações de edição/exclusão em uma linha própria. A contagem automática e a edição por ID permanecem inalteradas.
- O card `Analisar alimento com IA` agora recebe `aria-busy` e um estado visual de processamento simples: uma linha fina percorre a borda em movimento rápido enquanto a requisição está pendente, sem criar espera artificial, além do texto auxiliar `Processando sua descrição...`. Não há glow nem pulso interno; a animação respeita `prefers-reduced-motion` via regra global e desaparece ao concluir ou falhar.
- Validação local: tela conferida em 320px no harness; confirmação atualizou `1/4`, os totais e o anel; análise simulada pela IA preencheu e adicionou o alimento extra. `npm run lint`, `npm run test:unit` (39), `npm run test:rules` (28), `npm run build`, `npm run check:production` e `git diff --check` concluídos sem falhas funcionais.
- Commit `85a0c3f` (`fix(dieta): sinaliza carregamento da analise`) foi enviado para `origin/QA` e promovido por fast-forward para `origin/PRD`. O Firebase Hosting publicou com sucesso em https://akrgym.web.app; smoke test de Home, `robots.txt` e `sitemap.xml` retornou `200`. A publicação usou a conta de serviço local já protegida e nenhum segredo foi commitado.
